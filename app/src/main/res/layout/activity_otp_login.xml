<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#F4E1E7"
    tools:context=".OtpLogin">

    <!-- Title -->
    <TextView
        android:id="@+id/idTxtTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:fontFamily="@font/poppins_medium"
        android:gravity="center"
        android:text="OTP Verification"
        android:textColor="#333333"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- Caption for phone number -->
    <TextView
        android:id="@+id/idTxtPhoneNumberCaption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:fontFamily="@font/poppins_medium"
        android:text="Enter your phone number :"
        android:textColor="@color/black"
        android:textSize="16sp" />

    <!-- Edittext for getting user's phone number -->
    <EditText
        android:id="@+id/idEdtPhoneNumber"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/edittext_bg"
        android:fontFamily="@font/poppins_medium"
        android:hint="  Phone Number"
        android:textColor="#333333"
        android:textColorHint="#666666"
        android:textSize="16sp" />

    <!-- Caption for OTP -->
    <TextView
        android:id="@+id/idTxtOtpCaption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:fontFamily="@font/poppins_medium"
        android:text="Enter the OTP :"
        android:textColor="@color/black"
        android:textSize="16sp" />

    <!-- Edittext for getting OTP from user -->
    <EditText
        android:id="@+id/idEdtOtp"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/edittext_bg"
        android:fontFamily="@font/poppins_medium"
        android:hint=" OTP"
        android:textColor="#333333"
        android:textColorHint="#666666"
        android:textSize="16sp" />

    <!-- Button for getting OTP -->
    <Button
        android:id="@+id/idBtnGetOtp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:background="#2196F3"
        android:fontFamily="@font/poppins_medium"
        android:text="Get OTP"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Button for verifying user OTP -->
    <Button
        android:id="@+id/idBtnVerify"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#4CAF50"
        android:fontFamily="@font/poppins_medium"
        android:text="Verify OTP"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

</LinearLayout>
